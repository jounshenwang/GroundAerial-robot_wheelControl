# ESP32-S3 ↔ Jetson Orin Nano 硬件连接指南

> **用途**：涵洞巡检机器人上下位机串口通信
> **架构**：ESP32-S3 做实时控制（电机/编码器/安全），Orin Nano 做决策（Nav2/SLAM/视觉）
> **协议**：[简约二进制帧协议 7+15 byte](src/comm.h)，921600 bps，50Hz

---

## 一、引脚连接图

```
┌────────────────────────────────┐       ┌────────────────────────────────┐
│      Jetson Orin Nano          │       │      ESP32-S3-DevKitM-1        │
│      40-Pin Header             │       │       UART1 (Serial1)          │
│                                │       │                                │
│  Pin 8  (UART1_TXD)  ──────────┼──────▶│  GPIO44  (UART1_RX)            │
│  Pin 10 (UART1_RXD) ◀──────────┼───────│  GPIO43  (UART1_TX)            │
│  Pin 6  (GND)        ──────────┼───────│  GND                           │
│                                │       │                                │
│  电平: 3.3V ✓                  │       │  电平: 3.3V ✓                  │
│  (直连, 不需电平转换)          │       │  (直连, 不需电平转换)          │
└────────────────────────────────┘       └────────────────────────────────┘
```

### 实物接线（3 根杜邦线）

| Orin Nano 40-Pin | 线色 | ESP32-S3 |
|:---|:---|:---|
| Pin 8 (TXD) | 白/黄 → | GPIO44 (RX) |
| Pin 10 (RXD) | 绿 ← | GPIO43 (TX) |
| Pin 6 (GND) | 黑 ↔ | GND |

> ⚠️ **只接 3 根线**：TX、RX、GND。**不接 VCC**（两边各自独立供电）。

---

## 二、引脚详细说明

### ESP32-S3 侧

使用 **UART1 (Serial1)**，与 USB 调试串口 (Serial) 独立。

| ESP32-S3 引脚 | 功能 | 方向 | 说明 |
|:---|:---|:---:|:---|
| **GPIO44** | UART1 RX | ← 输入 | 接 Orin Nano Pin 8 TX |
| **GPIO43** | UART1 TX | → 输出 | 接 Orin Nano Pin 10 RX |
| **GND** | 地 | ↔ | 必须共地 |

代码中对应于 `src/config.h`：
```cpp
#define COMM_RX_PIN      44     // UART1 RX
#define COMM_TX_PIN      43     // UART1 TX
```

### Jetson Orin Nano 侧

使用 40-Pin 排针上的 **UART1**（设备路径 `/dev/ttyTHS1`）。

| Orin Nano 引脚 | 功能 | 方向 | 说明 |
|:---|:---|:---:|:---|
| **Pin 8** | UART1 TXD | → 输出 | 接 ESP32 GPIO44 RX |
| **Pin 10** | UART1 RXD | ← 输入 | 接 ESP32 GPIO43 TX |
| **Pin 6** | GND | ↔ | 必须共地 |

> 如果 40-Pin 排针的 UART 被其他服务占用，也可使用 USB 转 TTL 模块：
> - Orin Nano USB-A → USB-TTL → ESP32 UART1
> - 设备路径变为 `/dev/ttyUSB0`

---

## 三、通信参数

| 参数 | 值 | 说明 |
|:---|:---|:---|
| **接口** | UART1 | ESP32: Serial1 (GPIO44/43) |
| **波特率** | **921600** | 推荐，7 字节下行 ~60µs，15 字节上行 ~130µs |
| **数据位** | 8 | — |
| **校验位** | 无 (N) | 帧内 XOR 校验替代硬件校验 |
| **停止位** | 1 | — |
| **流控制** | 无 | 定长帧，50Hz 轮询，无需硬件流控 |
| **备用波特率** | 115200 | 如需更长距离/更稳定，双方同步修改 `COMM_BAUD` 即可 |
| **通信周期** | **20ms (50Hz)** | 与控制周期同步 |

---

## 四、帧格式简述

### 下行（Orin Nano → ESP32）7 字节

| 偏移 | 字段 | 类型 | 说明 |
|:---|:---|:---|:---|
| 0 | SYNC | uint8 | 帧头 `0xAA` |
| 1-2 | L_VEL | int16 | 左轮目标速度（脉冲/控制周期） |
| 3-4 | R_VEL | int16 | 右轮目标速度 |
| 5 | MODE | uint8 | 模式: 0=释放 1=手动 2=ROS2 3=急停 |
| 6 | CHECKSUM | uint8 | 前 6 字节 XOR |

### 上行（ESP32 → Orin Nano）15 字节

| 偏移 | 字段 | 类型 | 说明 |
|:---|:---|:---|:---|
| 0 | SYNC | uint8 | 帧头 `0xBB` |
| 1-4 | ENC_L | int32 | 左编码器累计脉冲数 |
| 5-8 | ENC_R | int32 | 右编码器累计脉冲数 |
| 9-10 | PITCH | int16 | 俯仰角 ×100（°） |
| 11-12 | FAULT | uint16 | 故障位掩码 |
| 13 | STATE | uint8 | 安全状态枚举 |
| 14 | CHECKSUM | uint8 | 前 14 字节 XOR |

详细协议定义见 [src/comm.h](src/comm.h)。

---

## 五、供电方案

```
┌─────────────────────────┐
│     电池 (12V~24V)       │
│         ◆                │
│    ┌────┴────┐           │
│    │  BEC /  │           │
│    │ DC-DC   │           │
│    │ 12V→5V  │           │
│    └────┬────┘           │
│         │ 5V             │
│    ┌────┴────┐           │
│    │    ...  │           │
│    │ 其他设备│           │
│    └─────────┘           │
├─────────────────────────┤
│  ESP32-S3  (USB 供电)    │ ← 5V 来自板载 USB 或 BEC
│  Orin Nano (DC 供电)     │ ← 独立电源，或同一电池经不同 DC-DC
└─────────────────────────┘
```

- **ESP32-S3**：通过 USB 供电（开发板自带 5V→3.3V LDO），或在 `5V`/`VIN` 引脚输入 5V
- **Jetson Orin Nano**：使用官方 DC 电源（建议 19V/4.74A 起）
- 两边 **GND 必须共地**（否则串口通信会不稳定），只需接一根 GND 线

---

## 六、系统框图

```
┌──────────────────────────────────────────────────────────┐
│  Orin Nano (ROS2 Humble) — 决策层                         │
│  ┌──────────┐  ┌──────────┐  ┌───────────────────────┐   │
│  │FAST-LIO2 │  │  Nav2    │  │culvert_mode_manager   │   │
│  │ (SLAM)   │  │ (规划)   │  │ (模式管理)             │   │
│  └──────────┘  └────┬─────┘  └───────────┬───────────┘   │
│                     │                    │                │
│               /cmd_vel              /mode_cmd             │
│                     │                    │                │
│              ┌──────┴────────────────────┴──────────┐     │
│              │      serial_bridge (serial_bridge.py) │     │
│              │  50Hz TX: 速度+模式 / RX: 编码器+姿态 │     │
│              └─────────────┬────────────────────────┘     │
└────────────────────────────┼──────────────────────────────┘
                    UART (/dev/ttyTHS1)
┌────────────────────────────┼──────────────────────────────┐
│  ESP32-S3 (PlatformIO) — 执行层 + 安全层                  │
│              ┌────────────┴────────────────────────┐      │
│              │  comm.cpp (NEW)                     │      │
│              │  50Hz RX: 速度+模式 / TX: 编码器+姿态│      │
│              └────────────┬────────────────────────┘      │
│  ┌──────────┐  ┌──────────┴──────┐  ┌──────────────────┐  │
│  │ encoder  │  │   main.cpp      │  │  safety.cpp      │  │
│  │ (ISR)    │  │   控制源分流     │  │  急停/倾角/堵转   │  │
│  └──────────┘  └────┬───────────┘  └──────────────────┘  │
│              ┌──────┴──────┐                              │
│              │  pid.cpp    │                              │
│              │  速度环 PID  │                              │
│              └──────┬──────┘                              │
│              ┌──────┴──────┐                              │
│              │  motor.cpp  │                              │
│              │  TB6612 ×4  │                              │
│              └─────────────┘                              │
│  ┌──────────┐                                            │
│  │ MPU6050  │ → 本地重力前馈 + 倾角安全（不上报上位机）    │
│  └──────────┘                                            │
└──────────────────────────────────────────────────────────┘
```

---

## 七、上电顺序与调试

### 推荐上电顺序

1. **ESP32-S3 先上电** → 自检 (POST) → 进入 SAFETY_NORMAL
2. 上电后默认 `MODE_MANUAL`，机器人由遥控器控制
3. **Orin Nano 后上电** → 启动 ROS2 → 启动 `serial_bridge` 节点
4. `serial_bridge` 连通后，通过 `/mode_cmd` 将模式设为 `MODE_ROS2_AUTO`
5. ESP32 收到模式切换 → 自动复位 PID → 开始接收速度指令

### 接线验证

ESP32 USB 串口监视器输出示例（`Serial.print` 调试信息）：

```
======= V12 Modular (4-MOTOR + MPU6050) =======
═══ Safety POST ═══
  [OK]  Receiver signals valid
  [OK]  Watchdog armed (3s timeout)
  >>> POST PASSED, entering NORMAL
═══ POST End ═══
```

连接 Orin Nano 串口后，ESP32 串口不会显示额外的通信日志（通信帧在 UART1，调试输出走 USB Serial），验证成功连接的方法：

- 将 Orin Nano Pin 8 (TX) 接到 ESP32 GPIO44 后，用示波器/logic analyzer 可测到 **921600 bps 的 0xAA 帧头发送**
- 或通过 Orin Nano 端的 `serial_bridge` 日志确认收到上行帧

---

## 八、常见问题排查

| 现象 | 可能原因 | 解决办法 |
|:---|:---|:---|
| ESP32 收不到下行帧 | RX/TX 接反 | 交换 ESP32 GPIO44/43 上的两根线 |
| 数据乱码 | 波特率不匹配 | 确认双方 `COMM_BAUD` 一致（默认 921600） |
| 通信间歇性断开 | GND 未共地 | 确认 GND 线已连接 |
| 模式无法切到 ROS2 | 串口超时保护 | Orin Nano 启动前先确认 ESP32 正在发上行帧 |
| ESP32 自动切回手动模式 | 超过 200ms 未收到帧 | 检查 Orin Nano serial_bridge 是否运行正常 |

---

> **相关文档**：
> - 通信协议定义：[src/comm.h](src/comm.h)
> - 通信模块实现：[src/comm.cpp](src/comm.cpp)
> - 架构分工分析：`C:\Users\Administrator\Desktop\涵洞巡检机器人方案\ESP32与ROS2架构分工.md`
> - 完整方案：`C:\Users\Administrator\Desktop\涵洞巡检机器人方案\涵洞巡检机器人完整方案.md`
