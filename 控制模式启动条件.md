# 控制模式启动条件

> 底盘共定义 **5 种控制模式**，各模式按固定优先级分流。  
> 核心代码：[main.cpp](src/main.cpp) 第 103-264 行（`loop()` 中的 C~E 节）。

---

## 全局前提条件

所有模式均受以下条件约束：

| 条件 | 说明 |
|------|------|
| **安全状态非急停** | `Safety::state != SAFETY_ESTOP`，否则所有电机动力切断 |
| **必须为地面模式** | **CH9 > 1700**（遥控器飞行/地面开关拨到"地面"侧） |
| **通信初始化** | 若需 ROS2 模式，必须先启用 `Comm::commInit()` 和 `Comm::commUpdate()` |

> 在飞行模式（CH9 ≤ 1700）下，所有电机完全释放（`motorStopAll`），PID 复位，位置目标跟随编码器，等待切回地面模式。

---

## 模式优先级总览

| 优先级 | 模式 | 控制方式 |
|--------|------|----------|
| **①** | **ROS2 自主导航** | 纯速度环，速度指令来自 Jetson Orin Nano 串口 |
| **②** | **手动遥控** | 串级 PID（位置环+速度环），指令来自遥控器摇杆 |
| **③** | **自主"弓"字形清扫** | 位置环+速度环，自动分阶段直行/转向/换行 |
| **④** | **静止驻车** | 目标锁定当前位置，电机维持零速 |
| **⑤** | **急停** | 所有电路直接刹车（独立于以上分流，由安全模块控制） |

---

## 模式 ①：ROS2 自主导航

**优先级：** 最高

**启动条件（全部满足）：**

| 条件 | 来源 | 值 |
|------|------|----|
| CH9 | 遥控器 | **> 1700**（地面模式） |
| 安全状态 | Safety 模块 | 非 `SAFETY_ESTOP` |
| Jetson 下行帧 mode 字段 | UART1 (Comm 模块) | **`MODE_ROS2_AUTO` = 2** |
| 通信链路 | UART1 | 200ms 内有有效下行帧（无超时） |

**行为：**
- 进入时自动复位 PID，清除上一模式残余积分
- **纯速度环控制**，不经过位置环
- 目标速度直接来自 Jetson 的 `vTargetL` / `vTargetR`（已限幅至动态最大值）
- 重力前馈保持生效（基于 IMU 俯仰角补偿）
- **退出时**自动对齐位置目标到当前编码器值 + 复位 PID，避免位置环阶跃

**退出条件（任一满足）：**

| 条件 | 后果 |
|------|------|
| Jetson 发送 `mode ≠ 2` | 进入对应低优先级模式 |
| 通信超时（> 200ms 无有效帧） | 自动降级为手动模式，速度归零 |
| 遥控器反馈急停 | 进入急停 `SAFETY_ESTOP` |
| CH9 ≤ 1700 | 切换为飞行模式，释放电机 |

**注：** ROS2 模式不受遥控器摇杆位置影响（油门/转向杆不会抢占控制权）。

---

## 模式 ②：手动遥控

**优先级：** 2

**启动条件（全部满足）：**

| 条件 | 来源 | 值 |
|------|------|----|
| Jetson 下行帧 mode | UART1 | **≠ `MODE_ROS2_AUTO`**（即非 2） |
| CH2（油门）或 CH1（转向） | 遥控器 | 超出死区（`±80` / `±50` µs） |
| CH2 有效范围 | 遥控器 | 900 < CH2 < 2100 |
| CH9 | 遥控器 | **> 1700**（地面模式） |
| 安全状态 | Safety 模块 | 非 `SAFETY_ESTOP` |

**行为：**
- **串级 PID**（位置环 + 速度环）
- 摇杆映射：
  - CH2（油门）→ `map(1000~2000, -50~50)` → 前进/后退速度步进
  - CH1（转向）→ `map(1000~2000, 25~-25)` → 转向步进（反向映射，右偏左转）
  - 位置目标 = 编码器计数 ± 转向合成步进
- 重力前馈淡入（`FF_FADE_STEP` = 0.02/周期，约 50 周期 = 1s 完成）
- 斜坡自适应（坡度增大时降低最大速度、增大比例增益）

**退出条件：**
- 摇杆回中（CH1 和 CH2 均在死区内）→ 进入静止驻车
- CH10 > 1600 → 进入自主清扫（若 Jetson 未发送 ROS2 模式）
- Jetson 发送 `mode=2` → ROS2 模式（更高优先级）
- 急停触发 → `SAFETY_ESTOP`

---

## 模式 ③：自主"弓"字形清扫

**优先级：** 3

**启动条件（全部满足）：**

| 条件 | 来源 | 值 |
|------|------|----|
| Jetson 下行帧 mode | UART1 | **≠ `MODE_ROS2_AUTO`** |
| CH1 / CH2 | 遥控器 | 均在死区内（摇杆回中） |
| **CH10** | 遥控器 | **> 1600**（清洁开关打开） |
| CH9 | 遥控器 | **> 1700**（地面模式） |
| 安全状态 | Safety 模块 | 非 `SAFETY_ESTOP` |

**行为：**
- 进入时自动从 `CLEAN_IDLE` → `LINE_FORWARD` 开始
- 按"弓"字形路径自动行驶：直行 → 90° 右转 → 横移 → 90° 右转 → 反向直行 → 90° 左转 → 横移 → 90° 左转 → 下一行
- 清洁电机 CH10 同时驱动
- 位置限幅保护（`ERROR_LIMIT = 800` 脉冲）

**完整状态序列：**

```
CLEAN_IDLE → LINE_FORWARD → TURN_RIGHT_1 → SHIFT_RIGHT → TURN_RIGHT_2
           → LINE_BACKWARD → TURN_LEFT_1 → SHIFT_LEFT → TURN_LEFT_2
           → (下一行) 或 → CLEAN_DONE
```

**退出条件（任一满足）：**
- CH10 ≤ 1600 → 回到静止驻车
- 摇杆动作（CH1/CH2 超出死区）→ 手动遥控
- Jetson 发送 `mode=2` → ROS2 模式
- 急停触发 → `SAFETY_ESTOP`
- 降级模式（`SAFETY_DEGRADED`）→ 自动复位清扫状态机
- 总行数达到 `MAX_ROWS = 6` → `CLEAN_DONE` 安全驻车

---

## 模式 ④：静止驻车

**优先级：** 最低（默认状态）

**启动条件（全部满足）：**

| 条件 | 来源 | 值 |
|------|------|----|
| Jetson 下行帧 mode | UART1 | **≠ `MODE_ROS2_AUTO`** |
| CH1 / CH2 | 遥控器 | 均在死区内 |
| CH10 | 遥控器 | **≤ 1600**（清洁开关关闭） |
| CH9 | 遥控器 | **> 1700**（地面模式） |
| 安全状态 | Safety 模块 | 非 `SAFETY_ESTOP` |

**行为：**
- 位置目标锁定当前编码器位置
- 串级 PID 持续运行，电机维持零速（抵抗外力扰动）
- 重力前馈保持生效
- 清洁电机停止

---

## 模式 ⑤：急停

**触发来源：** 安全监控模块（[safety.cpp](src/safety.cpp)）

**触发条件（任一满足）：**

| 条件 | 阈值 | 说明 |
|------|------|------|
| 倾角超限 | `> 45°` | MPU6050 检测 |
| 接收机信号丢失 | 500ms 无有效 PWM 信号 | 所有通道冻结 |
| 左轮堵转 | PWM 输出 ≥ 300 持续 ≥ 1000ms 且速度 ≈ 0 | 堵转检测 |
| 右轮堵转 | 同上 | 同左轮 |
| 看门狗超时 | 3000ms 未喂狗 | 硬件复位 |
| 手动触发 | `Safety::triggerEStop(reason)` | 程序或 Jetson 请求 |

**行为：**
- 所有电机输出 PWM 占空比 100% 对拉刹车（主动制动）
- 等待遥控器复位手势恢复（CH9 飞行→地面切换一次）
- ROS2 模式被无条件打断
- 急停期间清洁电机不工作

---

## 模式切换总图

```
                          ┌──────────────┐
                          │  飞行模式 F   │  (CH9 ≤ 1700)
                          │  电机释放     │
                          └──────┬───────┘
                                 │ CH9 > 1700
                                 ▼
                          ┌──────────────┐
                          │  地面模式 G   │
                          └──────┬───────┘
                                 │
              ┌──────────────────┼──────────────────┐
              ▼                  ▼                  ▼
     ┌────────────────┐ ┌──────────────┐ ┌────────────────┐
     │  Jetson 发     │ │ 遥控器动作   │ │  CH10 > 1600   │
     │  mode=2?       │ │  超出死区?   │ │  + 摇杆回中    │
     └───────┬────────┘ └──────┬───────┘ └───────┬────────┘
             │ YES             │ YES              │ YES
             ▼                 ▼                  ▼
     ┌────────────────┐ ┌──────────────┐ ┌────────────────┐
     │ ① ROS2 自主导航 │ │ ② 手动遥控    │ │ ③ 自动清扫     │
     │   (纯速度环)    │ │ (位置+速度环) │ │ (位置+速度环)  │
     └────────────────┘ └──────────────┘ └────────────────┘
             │                 │                  │
             └────────┬────────┴──────────┬───────┘
                      │   都不满足         │
                      ▼                   ▼
              ┌────────────────┐  ┌────────────────┐
              │ ④ 静止驻车     │  │ ⑤ 急停         │
              │ (位置锁定)     │  │ (主动刹车)     │
              └────────────────┘  └────────────────┘
```

---

## 引脚与遥控器通道映射

| 通道 | 引脚 | 功能 |
|------|------|------|
| CH1  | GPIO 14 | 转向摇杆（左右） |
| CH2  | GPIO 13 | 油门摇杆（前后） |
| CH9  | GPIO 12 | 飞行/地面模式切换（低=飞行，高=地面） |
| CH10 | GPIO 11 | 清洁开关 & 自动清扫触发 |

> 模式判断阈值（config.h）：  
> `THROTTLE_DEADBAND = 80`（油门死区 ±80 µs）  
> `STEERING_DEADBAND = 50`（转向死区 ±50 µs）
