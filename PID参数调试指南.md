# PID 参数调试指南 — ESP32-S3 陆空两栖机器人 V11

## 一、参数分类

### 需实地标定的参数

| 参数 | 默认值 | 说明 | 调整依据 |
|------|--------|------|---------|
| `AUTO_LINE_PULSES` | 8000 | 单行纵向直行距离 | 光伏面板实际长度 |
| `AUTO_TURN_PULSES` | 1150 | 原地自旋90度所需脉冲数 | 斜坡滑移程度，需现场微调 |
| `AUTO_ROW_PULSES` | 1500 | 换行横向平移距离 | 毛刷有效清洁宽度 |
| `AUTO_SPEED` | 15 | 自动直行每周期步进 | 清洁效果与电机负载平衡 |
| `AUTO_TURN_SPEED` | 12 | 自动自旋每周期步进 | 转向平滑度 |
| `MAX_ROWS` | 6 | 光伏板总行数 | 实际面板行数 |
| `MAX_RPM` | 250.0 | 电机额定转速上限 | 电机规格书 |

### 系统保护参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `ERROR_LIMIT` | 800 | 位置环误差保护限幅（脉冲），过大易失控，过小爬坡无力 |
| `I_V_LIMIT` | 100 | 速度环积分限幅，防止积分饱和 |
| `MIN_RPM_FACTOR` | 0.4667 | 陡坡时保留的最小速比（70/150） |

---

## 二、串级 PID 结构

```
                  位置环(外环)                    速度环(内环)
目标位置 ──→ [kp_p + ki_p/s] ──→ 目标速度 ──→ [kp_v + ki_v/s + kd_v·s] ──→ 电机PWM
       ↑                          ↑                                ↑
       │                          │                                │
   实际位置(编码器累加)      目标速度                           实际速度(编码器差分)
```

- **位置环**：计算"要跑多快才能到目标位置"
- **速度环**：计算"要输出多少PWM才能维持目标速度"
- **内环速度环必须远快于外环位置环**，否则系统振荡

---

## 三、速度环调参（内环，先调）

速度环决定电机"跟不跟手、有没有力"。

### 3.1 `kp_v`（默认 8.5）— 比例增益

**作用**：对速度误差的即时响应，决定了电机响应的"硬度和脆劲"。

| 过大的表现 | 过小的表现 |
|-----------|-----------|
| 电机高频抖动、尖锐啸叫、发热严重 | 响应迟钝，上坡速度明显掉，感觉"没力" |

**调试方法**：平地突然给阶跃目标（推摇杆到底），从当前值一半开始逐步增大，直到响应干脆但不抖动。最后退回 10-20% 作为安全裕量。

### 3.2 `ki_v`（默认 1.2）— 积分增益

**作用**：消除速度稳态误差，提供"持久力"。

| 过大的表现 | 过小的表现 |
|-----------|-----------|
| 低速抖动、来回小幅摆动、积分饱和导致失控 | 坡道速度持续偏低补不上来，坡度变化后恢复慢 |

**调试方法**：平地上先设为 0。然后在坡道上逐步加大，直到坡道速度偏差能在 1 秒内被补回来，且无低频摆动。

### 3.3 `kd_v`（默认 0.5）— 微分增益

**作用**：抑制速度突变，提供"阻尼"，减少超调和振荡。

| 过大的表现 | 过小的表现 |
|-----------|-----------|
| 电机反应"肉"，加减速拖泥带水，对指令响应慢 | 停止时明显过冲/回弹，加速时冲过头再拉回来 |

**调试方法**：反复突加速→突停止，观察电机响应。有超调就加，反应迟钝就减。

### 速度环调参口诀

> 平地给阶跃看响应 —— 干脆不抖即可。
> 上坡看速度维持 —— 不够就加 `ki_v`。
> 急停看回弹 —— 有超调加 `kd_v`，反应肉减 `kd_v`。

---

## 四、位置环调参（外环，后调）

位置环决定"停得准不准、到位快不快"。速度环调好后再调。

### 4.1 `kp_p`（默认 1.2）— 比例增益

**作用**：将位置误差转换为速度指令，决定了"追得有多猛"。

| 过大的表现 | 过小的表现 |
|-----------|-----------|
| 到位后来回摆动停不住，严重时整机前后低频振荡 | 到位过程很慢，停下来要"滑"一段距离才停住 |

**调试方法**：给一个中等距离的目标位置，从当前值逐步加大，直到开始振荡，然后退回 60-70%。

### 4.2 `ki_p`（默认 0.01）— 积分增益

**作用**：消除位置稳态误差，确保最终精确停在目标位置。

| 过大的表现 | 过小的表现 |
|-----------|-----------|
| 在目标点附近来回"画圈"摆动 | 差几个脉冲永远到不了目标，坡道尤其明显 |

**调试方法**：先设为 0。给目标位置后观察最终稳态——如果有残留偏差（尤其在坡上），逐步加 0.005 直到静止准确。通常不需超过 0.02。

### 4.3 位置环积分限幅（代码中 `-50, 50`）

位置积分限幅在 `computeCascadeFF()` 函数中设置：

```cpp
p.p_integral = constrain(p.p_integral + p_err, -50, 50);
```

| 限幅太小的表现 | 限幅太大的表现 |
|-------------|-------------|
| 坡道上位置追不上，持续有偏差 | 模式切换时产生冲击，积分累积后突然释放 |

---

## 五、完整调参流程

```
Step 1: 平地 → 调 kp_v
        推到不啸叫、不抖动的最大值，留 10-20% 裕量

Step 2: 坡道 → 调 ki_v
        上坡时速度不掉，且不出现低频摆动

Step 3: 加减速 → 调 kd_v
        停止时干脆不晃，加速时利落不冲

Step 4: 位置响应 → 调 kp_p
        跟得快但到位不摆动

Step 5: 停准检查 → 调 ki_p
        平地 & 坡道上都能精确停在目标位置
```

---

## 六、故障现象速查表

| 现象 | 最可能的原因 | 调整方向 |
|------|------------|---------|
| 整机低频前后振荡（周期 > 0.5s） | `kp_p` 太大 | 减小 `kp_p` |
| 电机高频啸叫（周期 < 0.1s） | `kp_v` 太大 | 减小 `kp_v` |
| 上坡无力、速度明显掉 | `ki_v` 太小或 `kp_v` 太小 | 先加 `kp_v`，再加 `ki_v` |
| 停到位后持续小幅度来回晃 | `ki_p` 太大或 `kp_p` 太大 | 先减 `kp_p`，无效则减 `ki_p` |
| 停不到位，差一点就停了 | `ki_p` 太小 | 加大 `ki_p` |
| 切换模式瞬间猛冲一下 | 位置积分限幅太大 | 减小限幅值 |
| 加速时冲过头再拉回 | `kd_v` 太小 | 加大 `kd_v` |
| 推杆后电机响应明显延迟 | `kd_v` 太大 | 减小 `kd_v` |
| 坡道上停不准 | `ki_p` 太小或位置积分限幅太小 | 加大 `ki_p`，放宽限幅 |
| 平地和坡道性能差距大 | `slope_factor` 动态增益不匹配 | 调整 `slope_factor * 4.0` 的系数 |

---

## 七、各参数文件位置

| 参数 | 文件 | 行号 |
|------|------|------|
| `kp_p`, `ki_p` | `v11.cpp` | 62 |
| `kp_v`, `ki_v`, `kd_v` | `v11.cpp` | 63 |
| `ERROR_LIMIT` | `v11.cpp` | 37 |
| `I_V_LIMIT` | `v11.cpp` | 38 |
| `MIN_RPM_FACTOR` | `v11.cpp` | 49 |
| 位置积分限幅(-50,50) | `v11.cpp` | `computeCascadeFF()` 函数内 |
| 动态 KP 增益系数(4.0) | `v11.cpp` | 284 |
| `AUTO_*` 系列参数 | `v11.cpp` | 52-57 |
