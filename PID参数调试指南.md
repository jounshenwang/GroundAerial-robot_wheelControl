# PID 参数调试指南 — ESP32-S3 陆空两栖机器人 

> **适用代码版本**: V12 Modular (模块化架构)
> **相关文件**: `src/config.h`, `src/pid.h`, `src/pid.cpp`, `src/main.cpp`

---

## 一、参数分类与文件位置

### 1.1 PID 核心参数 — [src/pid.h](src/pid.h)

`CascadePID` 结构体的默认值即为 PID 参数。运行时可通过 `pidL.kp_p = 新值` 直接修改。

| 参数 | 默认值 | 所属 | 说明 |
|------|--------|------|------|
| `kp_p` | 1.2 | 位置环 (外环) | 比例增益，将位置误差转换为速度指令 |
| `ki_p` | 0.01 | 位置环 (外环) | 积分增益，消除位置稳态误差 |
| `p_imax` | 50 | 位置环 (外环) | 积分限幅 (对称 ±50)，防止积分饱和 |
| `kp_v` | 8.5 | 速度环 (内环) | 比例增益 — ⚠ 运行时由坡度自适应动态覆盖 |
| `ki_v` | 1.2 | 速度环 (内环) | 积分增益，消除速度稳态误差 |
| `kd_v` | 0.5 | 速度环 (内环) | 微分增益 (微分先行，作用于实际速度) |
| `v_imax` | 100 | 速度环 (内环) | 积分限幅 (对称 ±100)，= `I_V_LIMIT` |

> **注意**: `kp_v` 的 struct 默认值 (8.5) 在运行时被坡度自适应计算覆盖，实际的 kp_v 在 `main.cpp` 中动态设置为 `KP_V_FLAT + slope * KP_V_SLOPE`。**调 `kp_v` 实际上调的是 `KP_V_FLAT`**。

### 1.2 控制与限制参数 — [src/config.h](src/config.h)

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `ERROR_LIMIT` | 800 | 位置环误差保护限幅 (脉冲)，过大易失控，过小爬坡无力 |
| `I_V_LIMIT` | 100 | 速度环积分限幅 (与 `v_imax` 保持一致) |
| `KP_V_FLAT` | 8.5 | **平地**速度环比例增益 |
| `KP_V_SLOPE` | 4.0 | **陡坡附加**速度环比例增益 (实际 kp_v = KP_V_FLAT + slope × KP_V_SLOPE) |
| `MIN_RPM_FACTOR` | 0.4667 | 陡坡时保留的最小速度比 (= 70/150)，防止爬坡飞车 |
| `CONTROL_PERIOD` | 20 | 控制周期 (ms)，改变此值需同步调整所有以周期为单位的参数 |

### 1.3 重力前馈参数 — [src/config.h](src/config.h)

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `PITCH_PWM_GAIN` | 500/60 ≈ 8.33 | 俯仰角→PWM 转换增益 (PWM/度) |
| `FF_MAP_MAX` | 45 | 前馈 PWM 最大值 (±45)，即前馈最多占总输出的 45/255 = 17.6% |
| `FF_FADE_STEP` | 0.02 | 前馈淡入步进/周期 (50周期 = 1秒完成淡入) |
| `FF_FADE_CYCLES` | 50 | 淡入完成所需周期数 |

### 1.4 自主清扫参数 — [src/config.h](src/config.h)

| 参数 | 默认值 | 说明 | 调整依据 |
|------|--------|------|---------|
| `AUTO_LINE_PULSES` | 8000 | 单行纵向直行距离 | 光伏面板实际长度 |
| `AUTO_TURN_PULSES` | 1150 | 原地自旋 90° 所需单侧脉冲 | 斜坡滑移程度，需现场微调 |
| `AUTO_ROW_PULSES` | 1500 | 换行横向平移距离 | 毛刷有效清洁宽度 |
| `AUTO_SPEED` | 15 | 自动直行每周期步进 | 清洁效果与电机负载平衡 |
| `AUTO_TURN_SPEED` | 12 | 自动自旋每周期步进 | 转向平滑度 |
| `MAX_ROWS` | 6 | 光伏板总行数 | 实际面板行数 |
| `MAX_RPM` | 250.0 | 电机额定转速上限 | 电机规格书 |

### 1.5 手动控制参数 — [src/config.h](src/config.h)

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `THROTTLE_DEADBAND` | 80 | 油门摇杆死区 (±µs，相对中位 1500) |
| `STEERING_DEADBAND` | 50 | 转向摇杆死区 (±µs) |
| `THROTTLE_STEP_MIN` | -50 | 油门最小步进 (后退) |
| `THROTTLE_STEP_MAX` | 50 | 油门最大步进 (前进) |
| `STEERING_STEP_MIN` | -25 | 转向最小步进 (左转) |
| `STEERING_STEP_MAX` | 25 | 转向最大步进 (右转) |

### 1.6 安全保护参数 — [src/config.h](src/config.h)

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `SAFETY_TILT_MAX` | 45.0° | 最大允许倾角，超限→急停 |
| `SAFETY_STALL_MS` | 500 | 堵转判定持续时长 (ms) |
| `SAFETY_STALL_PWM` | 250 | 视为堵转的持续饱和输出下限 |
| `SAFETY_PHASE_TIMEOUT` | 10000 | 自动清扫单阶段最大耗时 (ms) |
| `POS_OVERRUN_THRESHOLD` | 0.85 | 位置误差告警阈值 (占 ERROR_LIMIT 比例) |

---

## 二、串级 PID 结构

```
                  位置环(外环)                    速度环(内环)
目标位置 ──→ [kp_p + ki_p·∫] ──→ v_target ──→ [kp_v*+ ki_v·∫ + kd_v·D] ──→ PWM
       ↑              │              ↑              │              │
       │         ±maxV 限幅          │         ±255 限幅    重力前馈(+)
       │              │              │              │              │
   实际位置(编码器)   条件积分抗饱和   实际速度(编码器差分)   微分先行(D作用于测量值)
```

- **位置环 (外环)**: 计算"需要跑多快才能到达目标位置"
- **速度环 (内环)**: 计算"需要输出多少 PWM 才能维持目标速度"
- **内环速度环必须远快于外环位置环**，否则系统振荡
- \* `kp_v` 在运行时由坡度自适应动态计算: `kp_v = KP_V_FLAT + slope × KP_V_SLOPE`

### 坡度自适应增益

在 [src/main.cpp](src/main.cpp) 的每个控制周期中：

```cpp
// slope = 0 (平地) ~ 1 (极限陡坡)
float slope = abs(pitchPWM - 1500) / 500.0;

// 速度上限: 平地=100% → 陡坡=MIN_RPM_FACTOR(46.7%)
float dynMaxV = maxCPP - slope * maxCPP * (1 - MIN_RPM_FACTOR);

// 速度环P增益: 平地=KP_V_FLAT(8.5) → 陡坡=KP_V_FLAT+KP_V_SLOPE(12.5)
float dynKpV = KP_V_FLAT + slope * KP_V_SLOPE;
```

**设计意图**: 陡坡时增大比例增益提高"刚性"对抗重力，同时降低最高速度防止电机过热和失控。

---

## 三、速度环调参（内环，先调）

速度环决定电机"跟不跟手、有没有力"。**务必先调好速度环再调位置环。**

### 3.1 `KP_V_FLAT`（平地比例增益，默认 8.5）— 位于 [config.h:60](src/config.h#L60)

**作用**: 对速度误差的即时响应，决定了电机响应的"硬度和脆劲"。

| 过大的表现 | 过小的表现 |
|-----------|-----------|
| 电机高频抖动、尖锐啸叫、发热严重 | 响应迟钝，上坡速度明显掉，感觉"没力" |

**调试方法**: 在平地上**先将 `KP_V_SLOPE` 设为 0**（暂时禁用坡度自适应），然后突然给阶跃目标（推摇杆到底），从当前值的 50% 开始逐步增大，直到响应干脆但不抖动。最后退回 10-20% 作为安全裕量。调好后恢复 `KP_V_SLOPE`。

### 3.2 `KP_V_SLOPE`（陡坡附加增益，默认 4.0）— 位于 [config.h:61](src/config.h#L61)

**作用**: 坡度越大，速度环 P 增益越大。陡坡需要更大的"刚性"对抗重力分量。

| 过大的表现 | 过小的表现 |
|-----------|-----------|
| 坡道时电机高频抖动、发热 | 坡道上感觉"软"、速度掉得厉害 |

**调试方法**: 将机器置于最大工作坡度上，观察电机是否抖动。有抖动→减小 `KP_V_SLOPE`；坡道无力→增大 `KP_V_SLOPE`。

### 3.3 `ki_v`（默认 1.2）— 位于 [pid.h:18](src/pid.h#L18)

**作用**: 消除速度稳态误差，提供"持久力"。

| 过大的表现 | 过小的表现 |
|-----------|-----------|
| 低速抖动、来回小幅摆动、积分饱和导致失控 | 坡道速度持续偏低补不上来，坡度变化后恢复慢 |

**调试方法**: 平地上先将 `ki_v` 设为 0。然后在坡道上逐步加大，直到坡道速度偏差能在 1 秒内被补回来，且无低频摆动。

### 3.4 `kd_v`（默认 0.5）— 位于 [pid.h:19](src/pid.h#L19)

**作用**: 抑制速度突变，提供"阻尼"，减少超调和振荡。**本系统采用微分先行 (Derivative on Measurement)**，D 项只作用于实际速度的变化，对目标突跳不敏感。

| 过大的表现 | 过小的表现 |
|-----------|-----------|
| 电机反应"肉"，加减速拖泥带水，对指令响应慢 | 停止时明显过冲/回弹，加速时冲过头再拉回来 |

**调试方法**: 反复突加速→突停止，观察电机响应。有超调就加，反应迟钝就减。

### 3.5 `v_imax` / `I_V_LIMIT`（速度环积分限幅，默认 100）— 位于 [pid.h:20](src/pid.h#L20) 和 [config.h:45](src/config.h#L45)

**作用**: 防止速度环积分在长时间堵转或极限坡道上无限累积 (windup)。

| 限幅太小的表现 | 限幅太大的表现 |
|-------------|-------------|
| 坡道上速度持续偏低，积分不够用 | 长时间堵转后突然释放→电机猛冲 |

> **注意**: `v_imax` (pid.h 结构体成员) 和 `I_V_LIMIT` (config.h 宏) 应保持一致。修改时两处同步更新。

### 速度环调参口诀

> **平地给阶跃看响应** — 干脆不抖即可。
> **上坡看速度维持** — 不够就加 `KP_V_SLOPE` 和 `ki_v`。
> **急停看回弹** — 有超调加 `kd_v`，反应肉减 `kd_v`。

---

## 四、位置环调参（外环，后调）

位置环决定"停得准不准、到位快不快"。**速度环调好后再调。**

### 4.1 `kp_p`（默认 1.2）— 位于 [pid.h:11](src/pid.h#L11)

**作用**: 将位置误差转换为速度指令，决定了"追得有多猛"。

| 过大的表现 | 过小的表现 |
|-----------|-----------|
| 到位后来回摆动停不住，严重时整机前后低频振荡 | 到位过程很慢，停下来要"滑"一段距离才停住 |

**调试方法**: 给一个中等距离的目标位置，从当前值逐步加大，直到开始振荡，然后退回 60-70%。

### 4.2 `ki_p`（默认 0.01）— 位于 [pid.h:12](src/pid.h#L12)

**作用**: 消除位置稳态误差，确保最终精确停在目标位置。

| 过大的表现 | 过小的表现 |
|-----------|-----------|
| 在目标点附近来回"画圈"摆动 | 差几个脉冲永远到不了目标，坡道尤其明显 |

**调试方法**: 先将 `ki_p` 设为 0。给目标位置后观察最终稳态——如果有残留偏差（尤其在坡上），逐步加 0.005 直到静止准确。通常不需超过 0.02。

### 4.3 `p_imax`（位置环积分限幅，默认 50）— 位于 [pid.h:14](src/pid.h#L14)

**作用**: 位置环积分上限。在 [pid.cpp](src/pid.cpp) 的 `compute()` 中通过 `constrain(p_integral, -p_imax, p_imax)` 应用。

| 限幅太小的表现 | 限幅太大的表现 |
|-------------|-------------|
| 坡道上位置追不上，持续有偏差 | 模式切换时产生冲击，积分累积后突然释放 |

**调试方法**: 在最大工作坡度上观察位置跟踪——如果始终有偏差追不上，逐步放宽限幅（每次 +20）。如果切换模式/启动时猛冲，收紧限幅。

### 4.4 条件积分抗饱和 (Conditional Integration)

V12 采用**条件积分法**而非简单的积分限幅，位于 [pid.cpp:34-36](src/pid.cpp#L34-L36) 和 [pid.cpp:66-68](src/pid.cpp#L66-L68)：

```
当输出饱和 且 误差与积分同号 → 暂停积分（防止 windup）
当输出饱和 且 误差与积分异号 → 正常累积（快速退饱和）
当输出未饱和 → 正常累积
```

这意味着即使 `p_imax` 设置得较大，饱和时积分也不会无限制累积。当误差反转时积分能快速退出饱和状态。这一特性**不需要额外调参**，但理解它有助于诊断现象：
- 如果位置到位后摆动 → 不是积分 windup 的问题，应减 `kp_p` 或 `ki_p`
- 如果坡道长期偏差后突然纠正 → 检查 `p_imax` 是否太小

---

## 五、重力前馈调参

前馈系统位于 [main.cpp:186-187](src/main.cpp#L186-L187)：

```cpp
feedFwd = (pitchPWM - 1500) / 500 * FF_MAP_MAX * ff_fade;
```

### 5.1 `FF_MAP_MAX`（默认 45）— 位于 [config.h:66](src/config.h#L66)

**作用**: 前馈分量的最大幅值。值越大，坡道上的前馈补偿越强。

| 过大的表现 | 过小的表现 |
|-----------|-----------|
| 平地/浅坡时机车"自己往前冲"，停机时刹不住 | 坡道上电机需要 PID 积分来补，响应滞后 |

**调试方法**: 在中等坡度 (~15°) 上，先用 `ff_fade=0` 临时禁用前馈，记录稳态 PWM 输出。然后将 `FF_MAP_MAX` 设为该 PWM 值的 60-80%，让前馈承担大部分稳态负载，PID 只负责动态修正。

### 5.2 `FF_FADE_STEP`（默认 0.02）— 位于 [config.h:52](src/config.h#L52)

**作用**: 前馈从 0 到全量的淡入速率。值越大淡入越快。

| 过大的表现 | 过小的表现 |
|-----------|-----------|
| 空地切换瞬间电机猛冲 | 落地后前几秒坡道补偿不足，车往后溜 |

**调试方法**: 从飞行模式切换到地面模式的瞬间观察——如果猛冲，减小 `FF_FADE_STEP`；如果落地后明显溜坡且 2 秒后才稳住，增大 `FF_FADE_STEP`。

---

## 六、完整调参流程

```
Phase 0: 准备
  ├─ KP_V_SLOPE = 0 (暂时禁用坡度自适应)
  ├─ ki_v = 0, ki_p = 0 (归零积分)
  └─ FF_FADE_STEP = 0 (暂时禁用前馈)

Phase 1: 平地 → 调 KP_V_FLAT
  推到不啸叫、不抖动的最大值，留 10-20% 裕量

Phase 2: 坡道 → 调 KP_V_SLOPE
  在最大坡度上逐步增大，直到坡道有力但不抖动

Phase 3: 坡道 → 调 ki_v
  上坡时速度不掉，且不出现低频摆动

Phase 4: 加减速 → 调 kd_v
  停止时干脆不晃，加速时利落不冲

Phase 5: 位置响应 → 调 kp_p
  跟得快但到位不摆动

Phase 6: 停准检查 → 调 ki_p
  平地 & 坡道上都能精确停在目标位置

Phase 7: 位置积分限幅 → 调 p_imax
  坡道偏差不累积，切换模式不猛冲

Phase 8: 启用前馈 → 调 FF_MAP_MAX + FF_FADE_STEP
  中等坡度上先测稳态PWM→设 FF_MAP_MAX 为 60-80%；
  落地切换不猛冲即可

Phase 9: 综合验证
  平地 → 浅坡 → 陡坡各跑 5 分钟，确认无异常
```

---

## 七、故障现象速查表

| 现象 | 最可能的原因 | 调整方向 |
|------|------------|---------|
| 整机低频前后振荡 (周期 > 0.5s) | `kp_p` 太大 | 减小 `kp_p` |
| 电机高频啸叫 (周期 < 0.1s) | `kp_v` 太大 (或 `KP_V_SLOPE` 太大) | 减小 `KP_V_FLAT` 或 `KP_V_SLOPE` |
| 上坡无力、速度明显掉 | `KP_V_SLOPE` 太小或 `ki_v` 太小 | 先加 `KP_V_SLOPE`，再加 `ki_v` |
| 停到位后持续小幅度来回晃 | `ki_p` 太大或 `kp_p` 太大 | 先减 `kp_p`，无效则减 `ki_p` |
| 停不到位，差一点就停了 | `ki_p` 太小 | 加大 `ki_p` |
| 坡道上停不准 | `ki_p` 太小或 `p_imax` 太小 | 先加 `p_imax`，再加 `ki_p` |
| 切换模式瞬间猛冲一下 | `p_imax` 太大或 `FF_FADE_STEP` 太大 | 先减 `FF_FADE_STEP`，无效则减 `p_imax` |
| 加速时冲过头再拉回 | `kd_v` 太小 | 加大 `kd_v` |
| 推杆后电机响应明显延迟 | `kd_v` 太大 | 减小 `kd_v` |
| 落地瞬间车往后溜 | `FF_FADE_STEP` 太小 | 加大 `FF_FADE_STEP` |
| 平地和坡道性能差距大 | `KP_V_SLOPE` 不匹配 | 在目标坡度上单独调 `KP_V_SLOPE` |
| 陡坡上电机过热 | `MIN_RPM_FACTOR` 太大 (速度上限太高) | 减小 `MIN_RPM_FACTOR` |
| 陡坡上速度太慢 | `MIN_RPM_FACTOR` 太小 | 加大 `MIN_RPM_FACTOR` |
| 自动清扫时频繁触发 POS_OVERRUN | `ERROR_LIMIT` 太小 或 `AUTO_SPEED` 太大 | 加大 `ERROR_LIMIT` 或减小 `AUTO_SPEED` |
| 编码器计数但不动作 | 死区阈值 ±1 太小或电机驱动故障 | 检查 `motorSet()` 的 `out > 1` 阈值 |

---

## 八、各参数完整文件位置索引

| 参数 | 文件 | 行号 | 类型 |
|------|------|------|------|
| `kp_p` | [src/pid.h](src/pid.h) | 11 | struct 默认值 |
| `ki_p` | [src/pid.h](src/pid.h) | 12 | struct 默认值 |
| `p_imax` | [src/pid.h](src/pid.h) | 14 | struct 默认值 |
| `kp_v` (初始值) | [src/pid.h](src/pid.h) | 17 | struct 默认值 |
| `ki_v` | [src/pid.h](src/pid.h) | 18 | struct 默认值 |
| `kd_v` | [src/pid.h](src/pid.h) | 19 | struct 默认值 |
| `v_imax` | [src/pid.h](src/pid.h) | 20 | struct 默认值 |
| `KP_V_FLAT` | [src/config.h](src/config.h) | 60 | 宏 |
| `KP_V_SLOPE` | [src/config.h](src/config.h) | 61 | 宏 |
| `MIN_RPM_FACTOR` | [src/config.h](src/config.h) | 62 | 宏 |
| `ERROR_LIMIT` | [src/config.h](src/config.h) | 44 | 宏 |
| `I_V_LIMIT` | [src/config.h](src/config.h) | 45 | 宏 |
| `PITCH_PWM_GAIN` | [src/config.h](src/config.h) | 51 | 宏 |
| `FF_MAP_MAX` | [src/config.h](src/config.h) | 66 | 宏 |
| `FF_FADE_STEP` | [src/config.h](src/config.h) | 52 | 宏 |
| `THROTTLE_DEADBAND` | [src/config.h](src/config.h) | 56 | 宏 |
| `STEERING_DEADBAND` | [src/config.h](src/config.h) | 57 | 宏 |
| `AUTO_LINE_PULSES` | [src/config.h](src/config.h) | 92 | 宏 |
| `AUTO_TURN_PULSES` | [src/config.h](src/config.h) | 93 | 宏 |
| `AUTO_ROW_PULSES` | [src/config.h](src/config.h) | 94 | 宏 |
| `SAFETY_TILT_MAX` | [src/config.h](src/config.h) | 79 | 宏 |
| `SAFETY_STALL_MS` | [src/config.h](src/config.h) | 81 | 宏 |
| 坡度自适应计算 | [src/main.cpp](src/main.cpp) | 122-130 | 运行时 |
| 前馈计算 | [src/main.cpp](src/main.cpp) | 185-187 | 运行时 |
| 条件积分抗饱和 | [src/pid.cpp](src/pid.cpp) | 34-36, 66-68 | 算法 |
| 微分先行 | [src/pid.cpp](src/pid.cpp) | 55 | 算法 |

---

## 九、V11 → V12 变化对调参的影响

如果你从 V11 迁移到 V12，请注意以下变化：

| 变化点 | V11 (旧) | V12 (新) | 调参影响 |
|--------|----------|----------|---------|
| 架构 | 单文件 `v11.cpp` | 模块化 (`pid.cpp`, `config.h` 等) | 参数分散在多个文件中 |
| 积分策略 | 简单 `constrain` 限幅 | 条件积分抗饱和 | 积分限幅可以设得更宽松，不易 windup |
| 微分 | 误差微分 | 微分先行 (对测量值微分) | 目标突变不再产生微分冲击，可适当增大 `kd_v` |
| 速度环 P 增益 | 固定值 | 坡度自适应 (平地+坡道增量) | 拆分为 `KP_V_FLAT` 和 `KP_V_SLOPE` 两个参数 |
| 速度上限 | 固定 | 坡度自适应 (陡坡限速) | 新增 `MIN_RPM_FACTOR` 参数 |
| 前馈 | 无淡入 | 落地后 1 秒淡入 | 新增 `FF_FADE_STEP` 参数 |
| 调参文件引用 | 所有参数在 `v11.cpp` | 见第八章索引表 | 更新你的参数位置记录 |

---

> **文档版本**: v2.0 (适配 V12 Modular)
> **修订日期**: 2026-07-03
> **旧版适用**: V11 (见 git 历史 `bf7be83` 之前的版本)
