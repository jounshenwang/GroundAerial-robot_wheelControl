# 陆空两栖机器人底盘控制系统 — 代码说明报告

> **项目版本**: V12 Modular (4-MOTOR + MPU6050)
> **平台**: ESP32-S3-DevKitM-1 / PlatformIO / Arduino Framework
> **最后更新**: 2026-07-03

---

## 目录

1. [系统概述](#1-系统概述)
2. [硬件架构](#2-硬件架构)
3. [软件架构总览](#3-软件架构总览)
4. [模块详解](#4-模块详解)
   - [4.1 全局配置 — config.h](#41-全局配置--configh)
   - [4.2 主控入口 — main.cpp](#42-主控入口--maincpp)
   - [4.3 串级 PID 控制器 — pid.h / pid.cpp](#43-串级-pid-控制器--pidh--pidcpp)
   - [4.4 电机驱动 — motor.h / motor.cpp](#44-电机驱动--motorh--motorcpp)
   - [4.5 编码器 — encoder.h / encoder.cpp](#45-编码器--encoderh--encodercpp)
   - [4.6 遥控接收机 — receiver.h / receiver.cpp](#46-遥控接收机--receiverh--receivercpp)
   - [4.7 姿态传感器 — imu.h / imu.cpp](#47-姿态传感器--imuh--imucpp)
   - [4.8 自主清扫状态机 — autoclean.h / autoclean.cpp](#48-自主清扫状态机--autocleanh--autocleancpp)
   - [4.9 安全与可靠性 — safety.h / safety.cpp](#49-安全与可靠性--safetyh--safetycpp)
5. [控制流程](#5-控制流程)
6. [数据流图](#6-数据流图)
7. [关键设计决策](#7-关键设计决策)
8. [文件索引](#8-文件索引)

---

## 1. 系统概述

本项目是一个**陆空两栖机器人**的底盘运动控制系统，运行在 ESP32-S3 微控制器上。机器人在**地面模式**下通过四个独立驱动的轮毂电机实现全向移动，支持：

- **遥控手动操控**：通过 PX4 兼容的 PWM 接收机接收遥控器指令，实时控制底盘速度与方向
- **自主"弓"字形清扫**：内置状态机驱动的全自动路径规划，在光伏面板表面走蛇形轨迹完成清洁
- **重力前馈补偿**：通过 MPU6050 实时检测底盘倾角，向 PID 输出叠加前馈分量，主动对抗坡道重力分量
- **坡度自适应 PID**：速度环比例增益和速度上限根据当前坡度动态调整，陡坡时提升刚性并降低最高速度

飞行模式下底盘电机完全释放，由独立的飞控系统接管飞行。

---

## 2. 硬件架构

```
┌────────────────────────────────────────────────────────────┐
│                    ESP32-S3-DevKitM-1                       │
│                                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ CH1 (13) │  │ CH2 (14) │  │ CH9 (11) │  │CH10 (10) │   │
│  │  转向摇杆 │  │ 前进摇杆  │  │ 模式切换 │  │ 清洁开关 │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
│                     PX4 PWM 接收机                          │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              TB6612 × 4 路电机驱动                     │  │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  │  │
│  │  │ 左前     │  │ 左后     │  │ 右后     │  │ 右前     │  │  │
│  │  │ PWM:5   │  │ PWM:17  │  │ PWM:1   │  │ PWM:38  │  │  │
│  │  │IN1:7   │  │IN1:18  │  │IN1:2   │  │IN1:37  │  │  │
│  │  │IN2:6   │  │IN2:21  │  │IN2:42  │  │IN2:39  │  │  │
│  │  └─────────┘  └─────────┘  └─────────┘  └─────────┘  │  │
│  │                   STBY: GPIO4                          │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌────────────┐  ┌────────────┐  ┌──────────────────────┐  │
│  │ 左编码器    │  │ 右编码器    │  │   MPU6050 (I2C)      │  │
│  │ A:15 B:16 │  │ A:36 B:35 │  │ SDA:8  SCL:3       │  │
│  └────────────┘  └────────────┘  │ Addr: 0x68          │  │
│                                   └──────────────────────┘  │
│                                                             │
│  ┌────────────────────┐                                     │
│  │ 清洁电机 (继电器)   │                                     │
│  │ GPIO47             │                                     │
│  └────────────────────┘                                     │
└────────────────────────────────────────────────────────────┘
```

### 传感器与执行器规格

| 组件 | 型号/参数 | 说明 |
|------|----------|------|
| 底盘电机 | 直流减速电机 ×4 | 编码器 PPR=13, 减速比 34:1, 4倍频 |
| 电机驱动 | TB6612FNG ×4 | 5kHz PWM, 8bit 分辨率 |
| 编码器 | 霍尔编码器 | 双边沿中断，4倍频 = 52 CPR（轴端）/ 1768 CPR（输出端） |
| IMU | MPU6050 | I2C 400kHz, 互补滤波俯仰解算 |
| 接收机 | PX4 兼容 PWM | 4通道, CHANGE 中断测脉宽 |

---

## 3. 软件架构总览

项目采用**模块化分层架构**，共 9 个功能模块：

```
                    ┌──────────────┐
                    │   main.cpp   │  ← 主调度循环 (50Hz)
                    └──────┬───────┘
           ┌───────────────┼───────────────┐
           │               │               │
    ┌──────▼──────┐ ┌──────▼──────┐ ┌──────▼──────┐
    │   pid.cpp   │ │ autoclean   │ │  safety.cpp │
    │  串级 PID   │ │   .cpp      │ │  安全监控   │
    └──────┬──────┘ │ 清扫状态机  │ └──────┬──────┘
           │        └─────────────┘        │
    ┌──────▼───────────────────────────────▼──────┐
    │            底层驱动层                        │
    │  motor.cpp │ encoder.cpp │ receiver.cpp     │
    │  imu.cpp   │             │                  │
    └─────────────────────────────────────────────┘
    ┌─────────────────────────────────────────────┐
    │            配置层                            │
    │  config.h   (所有参数、引脚、常量)            │
    └─────────────────────────────────────────────┘
```

### 控制周期

- **主循环频率**: 50Hz (20ms 控制周期)
- **PID 输出频率**: 与主循环同步 (50Hz)
- **IMU 更新频率**: 与主循环同步 (50Hz)
- **串口输出频率**: 2Hz (500ms 间隔)
- **安全监控频率**: 与主循环同步

### 编译配置 (platformio.ini)

```ini
[env:esp32-s3-devkitm-1]
platform = espressif32
board = esp32-s3-devkitm-1
framework = arduino
monitor_speed = 115200
lib_deps = pololu/VL53L1X@^1.0.0
```

---

## 4. 模块详解

### 4.1 全局配置 — [config.h](src/config.h)

所有硬件引脚映射、控制常量、运动学参数集中管理。分为四大类：

#### (1) 硬件引脚映射

| 宏定义 | GPIO | 功能 |
|--------|------|------|
| `PX4_CH1_PIN` | 13 | 接收机 CH1 (转向摇杆) |
| `PX4_CH2_PIN` | 14 | 接收机 CH2 (前进/后退摇杆) |
| `PX4_CH9_PIN` | 11 | 接收机 CH9 (模式切换) |
| `PX4_CH10_PIN` | 10 | 接收机 CH10 (清洁开关) |
| `STBY_PIN` | 4 | TB6612 待机控制 |
| `PWMA_PIN` ~ `PWMD_PIN` | 5, 17, 1, 38 | 四路 PWM 输出 |
| `AIN1_PIN` ~ `DIN2_PIN` | 7,6, 18,21, 2,42, 37,39 | 四路方向控制 |
| `CLEAN_MOTOR_PIN` | 47 | 清洁电机继电器 |
| `ENC_L_A/B` | 15/16 | 左编码器 A/B 相 |
| `ENC_R_A/B` | 36/35 | 右编码器 A/B 相 |
| `MPU_SDA/SCL` | 8/3 | I2C 总线 |

#### (2) 常量与控制参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `ERROR_LIMIT` | 800 | 位置环误差保护限幅 (脉冲) |
| `I_V_LIMIT` | 100 | 速度环积分限幅 |
| `CONTROL_PERIOD` | 20 | 控制周期 20ms |
| `KP_V_FLAT` | 8.5 | 速度环比例增益 (平地) |
| `KP_V_SLOPE` | 4.0 | 速度环比例增益坡道增量 |
| `MIN_RPM_FACTOR` | 0.4667 | 陡坡保留最小速比 |
| `FF_FADE_STEP` | 0.02 | 前馈淡入步进 |
| `FF_MAP_MAX` | 45 | 前馈 PWM 最大值 |
| `THROTTLE_DEADBAND` | 80 | 油门死区 (±µs) |
| `STEERING_DEADBAND` | 50 | 转向死区 (±µs) |

#### (3) 安全与可靠性参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `SAFETY_TILT_MAX` | 45° | 最大允许倾角 |
| `SAFETY_SIGNAL_TIMEOUT` | 500ms | 接收机信号超时 |
| `SAFETY_STALL_MS` | 500ms | 堵转判定持续时长 |
| `SAFETY_STALL_PWM` | 250 | 堵转 PWM 下限 |
| `SAFETY_PHASE_TIMEOUT` | 10000ms | 清扫单阶段超时 |
| `WDT_TIMEOUT` | 3000ms | 看门狗超时 |

#### (4) 自主清扫参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `AUTO_LINE_PULSES` | 8000 | 单行直行距离 |
| `AUTO_TURN_PULSES` | 1150 | 90° 自旋脉冲数 |
| `AUTO_ROW_PULSES` | 1500 | 换行横移距离 |
| `AUTO_SPEED` | 15 | 直行速度步进 |
| `AUTO_TURN_SPEED` | 12 | 自旋速度步进 |
| `MAX_ROWS` | 6 | 总清扫行数 |

---

### 4.2 主控入口 — [main.cpp](src/main.cpp)

#### 全局状态变量

| 变量 | 类型 | 说明 |
|------|------|------|
| `targetPosL/R` | `long` | PID 位置环目标值 |
| `ff_fade` | `float` | 重力前馈淡入因子 (0→1) |
| `lastGround` | `bool` | 上一周期空地模式标志 |

#### setup() 初始化流程

```
Serial.begin(115200)
  → pinMode(CLEAN_MOTOR_PIN)
  → IMU::begin()        // MPU6050 I2C 初始化
  → Encoder::begin()    // 编码器中断绑定
  → Receiver::begin()   // 接收机中断绑定
  → motorBegin()        // LEDC PWM 配置
  → targetPosL/R 对齐  // PID 目标与编码器同步
  → Safety::runPOST()   // 上电自检
```

#### loop() 主循环流程 (50Hz)

```
0. 节拍控制 (CONTROL_PERIOD 20ms)
   ↓
1. Safety::feedWatchdog()  ← 无条件喂狗
   ↓
2. 快照接收机通道 (rxCh1/2/9/10)
   ↓
3. 外设控制: 清洁电机 + IMU::update() + 前馈计算
   ↓
4. 速度测量: 编码器差分 (speedL/R)
   ↓
5. 模式判断: groundMode = (rxCh9 > 1700)
   ↓
6. 急停处理: Safety::state == SAFETY_ESTOP?
   ├─ YES → tryRecover(), 跳过本周期
   └─ NO  → 进入正常控制
       ├─ 降级模式: 禁用自动清扫, 归零前馈
       ├─ 地面模式:
       │   ├─ 空地切换过渡 → 同步编码器, 复位 PID
       │   ├─ 坡度自适应 → 计算 slope, dynMaxV, dynKpV
       │   ├─ 控制源分流:
       │   │   ├─ 手动遥控 → moveStep/steerStep 来自摇杆
       │   │   ├─ 自主清扫 → autoCleanUpdate() 状态机
       │   │   └─ 静止驻车 → moveStep=steerStep=0
       │   ├─ 位置环目标更新 + 硬限幅
       │   ├─ 位置误差超限检测(带滞回)
       │   ├─ 串级 PID 计算 → outL/R
       │   └─ motorSetDifferential(outL, outR)
       └─ 飞行模式:
           └─ 释放所有电机, 复位 PID, 同步编码器
   ↓
7. 运行时安全监控: Safety::runtimeMonitor()
   ↓
8. 调试输出 (500ms 间隔)
```

#### 关键设计细节

1. **信号快照 (anti-tearing)**: `loop()` 入口处一次性快照 `Receiver::ch1/2/9/10` 到局部变量，后续全部使用局部变量，避免 ISR 在判断中途更新导致逻辑撕裂。

2. **速度测量初始化**: 首个控制周期用 `speedInitd` 标记，将 `lastCnt` 对齐到当前编码器计数，避免第一个速度值为异常跳变。

3. **位置误差超限滞回**: 超限时立即置位 `FAULT_POS_OVERRUN`；恢复正常后需连续 10 周期内均正常才清除，防止瞬态毛刺误清除。

4. **前馈浮点映射**: `pitchPWM → feedFwd` 采用浮点除法替换 `map()` 整数运算，避免阶梯状输出抖动。

---

### 4.3 串级 PID 控制器 — [pid.h](src/pid.h) / [pid.cpp](src/pid.cpp)

#### 控制结构

```
  目标位置 ──→ [位置环 P+I] ──→ v_target ──→ [速度环 P+I+D] ──→ PWM 输出
       ↑              │              ↑              │
       │         ±maxV 限幅         │          ±255 限幅
       │              │              │              │
   实际位置      重力前馈淡入     实际速度        重力前馈
```

#### PID 参数

| 参数 | 默认值 | 所属 | 说明 |
|------|--------|------|------|
| `kp_p` | 1.2 | 位置环 | 比例增益 |
| `ki_p` | 0.01 | 位置环 | 积分增益 |
| `p_imax` | 50 | 位置环 | 积分限幅 |
| `kp_v` | 8.5 (平地) | 速度环 | 比例增益 (坡度自适应) |
| `ki_v` | 1.2 | 速度环 | 积分增益 |
| `kd_v` | 0.5 | 速度环 | 微分增益 |
| `v_imax` | 100 | 速度环 | 积分限幅 |

#### 关键算法特性

**1. 条件积分抗饱和 (Conditional Integration Anti-Windup)**

两层抗饱和，位置环和速度环各一层：

```
if (!saturated || (error * integral < 0.0f)):
    integral += error   // 正常累积
else:
    skip                // 饱和且同向 → 暂停积分
integral = constrain(integral, -imax, +imax)
```

- **饱和且异号**：积分正在"退饱和"，允许累积以快速退出饱和
- **饱和且同号**：积分会加剧饱和，暂停累积
- 这种策略比简单的积分冻结更智能，既能防 windup 又不牺牲退饱和速度

**2. 微分先行 (Derivative on Measurement)**

```
D 项 = kd_v * (v_last_speed - actualSpeed)
     ≡ -kd_v * d(actualSpeed)/dt
```

传统的误差微分 `d(v_target - actual)/dt` 在 v_target 突变时会产生巨大的"微分冲击"(derivative kick)，导致电机瞬态抖动。改为对实际速度求微分后，目标突跳仅通过 P/I 通道响应，电机动作平滑无冲击。

**3. 坡度自适应**

在 `main.cpp` 的循环中动态计算：

```cpp
float slope = |pitchPWM - 1500| / 500;         // 0(平地) ~ 1(陡坡)
float dynMaxV = maxCPP - slope * maxCPP * (1 - MIN_RPM_FACTOR);  // 速度上限降低
float dynKpV  = KP_V_FLAT + slope * KP_V_SLOPE;  // 比例增益提高
```

- 平地 (slope=0): `kp_v = 8.5`, 速度上限 = 100%
- 陡坡 (slope=1): `kp_v = 12.5`, 速度上限 = 46.7%
- 效果：陡坡时增大力矩刚性但限制最高速度，防止电机过热和失控

**4. 重力前馈**

```cpp
float feedFwd = (pitchPWM - 1500) / 500 * FF_MAP_MAX * ff_fade;
```

- `pitchPWM`: 由俯仰角映射的 PWM 值 (1000~2000, 1500=水平)
- `ff_fade`: 从空地切换后从 0 逐步淡入到 1，避免刚落地时的前馈冲击
- 最终前馈范围: -45 ~ +45 (PWM 单位)

---

### 4.4 电机驱动 — [motor.h](src/motor.h) / [motor.cpp](src/motor.cpp)

#### 驱动方式

- **芯片**: TB6612FNG ×4 (每路独立控制一个电机)
- **PWM**: ESP32 LEDC 硬件 PWM, 5kHz, 8bit 分辨率 (0~255)
- **通道映射**: 通道0=左前, 通道1=左后, 通道2=右后, 通道3=右前

#### 核心函数

| 函数 | 功能 |
|------|------|
| `motorBegin()` | 初始化 STBY 使能、方向引脚、LEDC PWM |
| `motorSet(out, p1, p2, ch)` | 单路电机控制 (-255~255) |
| `motorSetDifferential(L, R)` | 差分驱动：同侧前后轮共用同一信号 |
| `motorStopAll(activeBrake)` | 全停：true=短路制动防溜坡, false=完全释放 |

#### 死区设计

```cpp
if (out > 1)       → 正向 (IN1=H, IN2=L)
else if (out < -1) → 反向 (IN1=L, IN2=H)
else               → 死区/释放 (IN1=L, IN2=L) 自由滑行
```

采用 ±1 死区且不施加短路制动 (IN 不同时为 H)，原因：短路制动会使 PID 小修正量被强行阻挡，积分持续累积后在跳出死区瞬间产生突跳式震荡。

#### 差分驱动逻辑

```
左前(outL, AIN1, AIN2, ch0)   ← 同一 PWM 信号
左后(outL, BIN1, BIN2, ch1)   ← 同一 PWM 信号
右后(outR, CIN1, CIN2, ch2)   ← 同一 PWM 信号
右前(outR, DIN1, DIN2, ch3)   ← 同一 PWM 信号
```

四轮差速：同侧前后轮并联同一控制信号，左右独立。

---

### 4.5 编码器 — [encoder.h](src/encoder.h) / [encoder.cpp](src/encoder.cpp)

#### 测量原理

- **传感器**: 霍尔编码器, PPR=13, 减速比 34:1
- **倍频**: A/B 相双边沿触发 (CHANGE 中断), 4 倍频
- **等效分辨率**: 输出轴每转 13×4×34 = **1768 脉冲/转**

#### ISR 方向判定

```cpp
// A相 ≠ B相 → 正向, A相 = B相 → 反向
if (digitalRead(A) != digitalRead(B)) count++;
else count--;
```

每次 A 相或 B 相的任意边沿变化都触发中断，共 4 个中断源（左右各 2 个）。

#### 原子读取

```cpp
static inline long readL() {
    noInterrupts();
    long val = count_l;  // 关中断防止 32-bit 变量被 ISR 撕裂
    interrupts();
    return val;
}
```

`volatile long` 在 ESP32 上为非原子类型（32-bit），必须关中断读取。`readPair()` 在同一临界区内读取左右值，保证时序一致性。

---

### 4.6 遥控接收机 — [receiver.h](src/receiver.h) / [receiver.cpp](src/receiver.cpp)

#### 信号格式

- **类型**: PX4 兼容 PWM (标准舵机信号)
- **周期**: ~20ms (50Hz)
- **脉宽范围**: 1000~2000 µs, 中位 1500 µs

#### 通道功能

| 通道 | 默认值 | 功能 |
|------|--------|------|
| CH1 | 1500 | 转向摇杆 (左右) |
| CH2 | 1500 | 前进/后退摇杆 |
| CH9 | 1000 | 模式开关 (≤1700 飞行 / >1700 陆地) |
| CH10 | 1000 | 清洁开关 (>1600 开启清洁电机 + 触发自动清扫) |

#### ISR 实现

```cpp
void IRAM_ATTR readCh1_ISR() {
    static uint32_t t = 0;
    if (digitalRead(PIN)) t = micros();           // 上升沿 → 记录时间
    else Receiver::ch1 = constrain(micros() - t, 1000, 2000);  // 下降沿 → 计算脉宽
}
```

每个引脚绑定 CHANGE 中断，测量高电平持续时间。`constrain` 硬限幅防止异常脉宽被下游逻辑误判。

---

### 4.7 姿态传感器 — [imu.h](src/imu.h) / [imu.cpp](src/imu.cpp)

#### 传感器配置

- **芯片**: MPU6050 (6 轴 IMU)
- **总线**: I2C, 400kHz Fast Mode
- **地址**: 0x68

#### 姿态解算

**互补滤波器**融合加速度计和陀螺仪数据：

```cpp
// 加速度计俯仰角 (低频准确, 高频噪声大)
accel_pitch = atan2(ay, sqrt(ax² + az²))

// 陀螺仪角速度积分 (高频准确, 有漂移)
gyro_rate = gy / 131.0  // ±250°/s 量程

// 互补滤波
angle = (1-α) * (angle + gyro_rate*dt) + α * accel_pitch
α = dt / (τ + dt)       // τ = 1s 时间常数
```

- **短 dt** → α 小 → 更偏向陀螺仪 (动态响应好)
- **长 dt** → α 大 → 更偏向加速度计 (静态无漂移)

#### I2C 总线恢复状态机

非阻塞三段式恢复：

```
I2C_OK → (连续 10 次错误) → I2C_RESETTING (等待 60ms)
       → I2C_WAKING (等待 50ms)
       → I2C_OK (标记 angleInitialized=false 重新初始化角度)
```

恢复期间 `dataValid=false`，会触发 `FAULT_IMU` 和紧急停止，确保不会使用过时的陀螺仪数据。

---

### 4.8 自主清扫状态机 — [autoclean.h](src/autoclean.h) / [autoclean.cpp](src/autoclean.cpp)

#### 状态转换图

```
                  ┌─────────────────────────────────┐
                  │         CLEAN_IDLE               │
                  │   (ch10 > 1600 触发开始)          │
                  └─────────────┬───────────────────┘
                                │
                  ┌─────────────▼───────────────────┐
                  │       LINE_FORWARD (上坡)        │
                  │   move=AUTO_SPEED, steer=0      │
                  └─────────────┬───────────────────┘
                                │ 直行距离 ≥ AUTO_LINE_PULSES
                  ┌─────────────▼───────────────────┐
                  │       TURN_RIGHT_1 (90°右转)     │
                  │   move=0, steer=AUTO_TURN_SPEED │
                  └─────────────┬───────────────────┘
                                │ 右轮脉冲 ≥ AUTO_TURN_PULSES
                  ┌─────────────▼───────────────────┐
                  │       SHIFT_RIGHT (换行平移)      │
                  │   move=AUTO_SPEED, steer=0      │
                  └─────────────┬───────────────────┘
                                │ 平移距离 ≥ AUTO_ROW_PULSES
                  ┌─────────────▼───────────────────┐
                  │       TURN_RIGHT_2 (90°右转)     │
                  │   move=0, steer=AUTO_TURN_SPEED │
                  └─────────────┬───────────────────┘
                                │   (面朝下坡)
                  ┌─────────────▼───────────────────┐
                  │      LINE_BACKWARD (下坡)        │
                  │   move=AUTO_SPEED, steer=0      │
                  └─────────────┬───────────────────┘
                                │
                  ┌─────────────▼───────────────────┐
                  │  奇数行? → TURN_LEFT 序列 (左转)  │
                  │  偶数行? → TURN_RIGHT 序列 (右转) │
                  │  rows ≥ MAX_ROWS? → CLEAN_DONE  │
                  └─────────────────────────────────┘
```

#### 状态枚举

| 状态 | 值 | 说明 |
|------|-----|------|
| `CLEAN_IDLE` | 0 | 待命/手动遥控 |
| `LINE_FORWARD` | 1 | 正向直行 (上坡方向) |
| `TURN_RIGHT_1` | 2 | 第1次右转 90° |
| `SHIFT_RIGHT` | 3 | 向右横移换行 |
| `TURN_RIGHT_2` | 4 | 第2次右转 90° (完成 180° 调头) |
| `LINE_BACKWARD` | 5 | 反向直行 (下坡方向) |
| `TURN_LEFT_1` | 6 | 第1次左转 90° |
| `SHIFT_LEFT` | 7 | 向左横移换行 |
| `TURN_LEFT_2` | 8 | 第2次左转 90° (完成 180° 调头) |
| `CLEAN_DONE` | 9 | 全部完成 |

#### 安全保护

- **阶段超时保护**: 单阶段超过 `SAFETY_PHASE_TIMEOUT` (10s) → 触发 `FAULT_POS_OVERRUN` + 自动复位
- **编码器起点锁定**: `changeAutoState()` 记录当前编码器位置，后续距离判断基于差值

---

### 4.9 安全与可靠性 — [safety.h](src/safety.h) / [safety.cpp](src/safety.cpp)

#### 安全状态机

```
SAFETY_INIT ──POST通过──→ SAFETY_NORMAL ──故障检测──→ SAFETY_ESTOP
                                │                         │
                                ├── 告警 ──→ SAFETY_WARNING │
                                │                         │
                                └── 降级 ──→ SAFETY_DEGRADED
                                                          │
                                              ch9 复位手势 ──→ SAFETY_NORMAL
```

| 状态 | 值 | 电机状态 | 自主清扫 | 前馈 |
|------|-----|---------|---------|------|
| `SAFETY_INIT` | 0 | 停止 | 禁用 | — |
| `SAFETY_NORMAL` | 1 | 正常 | 可用 | 正常 |
| `SAFETY_WARNING` | 2 | 正常 | 可用 | 正常 |
| `SAFETY_DEGRADED` | 3 | 正常 | 禁用 | 归零 |
| `SAFETY_ESTOP` | 4 | 短路制动 | 禁用 | 归零 |

#### 故障标志位 (位掩码)

| 标志 | 位 | 触发条件 | 严重级别 |
|------|-----|---------|---------|
| `FAULT_IMU` | 0 | I2C 故障/数据无效 | 降级 |
| `FAULT_ENCODER_L` | 1 | (预留) | 降级 |
| `FAULT_ENCODER_R` | 2 | (预留) | 降级 |
| `FAULT_RX_SIGNAL` | 3 | 信号丢失/冻结 | **致命→急停** |
| `FAULT_MOTOR_DRV` | 4 | (预留) | 降级 |
| `FAULT_BAT_LOW` | 5 | (预留) | 告警 |
| `FAULT_OVER_TILT` | 6 | 倾角 > 45° | **致命→急停** |
| `FAULT_STALL_L` | 7 | 左轮堵转 | **致命→急停** |
| `FAULT_STALL_R` | 8 | 右轮堵转 | **致命→急停** |
| `FAULT_POS_OVERRUN` | 9 | 位置误差超限 | 告警 (不直接触发急停) |

#### 运行时监控检测链 (每周期执行)

```
1. 急停状态下跳过所有检测
2. 接收机信号检测 (双层)
   ├─ 硬边界: 任一通道 <800 或 >2200 → 立即急停
   └─ 冻结检测: ch1/ch2 连续 20 次采样不变 ~2s → 急停
3. IMU 数据有效性检查
   └─ dataValid==false → 急停
4. 倾角超限检测
   └─ |angle_pitch| > 45° → 急停
5. 堵转检测 (滞回)
   └─ PWM饱和(≥250) + 速度≈0 持续 500ms → 急停
6. 状态降级判定
   ├─ IMU/编码器故障 → SAFETY_DEGRADED
   └─ 电池低压 → SAFETY_WARNING
```

#### 急停恢复手势

```
ch9 < 1200 (飞行档) → ch9 > 1700 (地面档) → 恢复
```

恢复时只清除瞬态故障 (OVERTILT/STALL/POSOVERRUN/RXSIGNAL)，保留硬件故障位 (IMU/ENCODER/MOTORDRV)，确保持续性硬件故障不会被意外清除。

---

## 5. 控制流程

### 5.1 手动遥控路径

```
摇杆 CH1/CH2 脉宽
  → map(1000~2000) → moveStep(-50~+50) / steerStep(-25~+25)
  → targetPosL += moveStep + steerStep
  → targetPosR += moveStep - steerStep
  → 位置硬限幅 constrain(target, cnt-800, cnt+800)
  → 串级 PID
  → motorSetDifferential(outL, outR)
```

### 5.2 自主清扫路径

```
autoCleanUpdate(cntL, cntR)
  → 状态机根据当前阶段返回 moveStep / steerStep
  → 与手动相同的 targetPos 更新 → PID → 电机输出
```

### 5.3 重力前馈路径

```
IMU::angle_pitch
  → pitchPWM = 1500 + angle * (500/60)
  → feedFwd = (pitchPWM-1500)/500 * 45 * ff_fade
  → 叠加到速度环 PID 输出
```

---

## 6. 数据流图

```
┌──────────────────────────────────────────────────────────────────┐
│                        50Hz 主循环                               │
│                                                                   │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐   │
│  │ Receiver │    │ Encoder  │    │   IMU    │    │autoclean │   │
│  │  (ISR)   │    │  (ISR)   │    │ (I2C)   │    │(状态机)  │   │
│  └────┬─────┘    └────┬─────┘    └────┬─────┘    └────┬─────┘   │
│       │               │               │               │          │
│  ch1,ch2         cntL,cntR        angle_pitch    moveStep,      │
│  ch9,ch10        speedL,speedR                    steerStep     │
│       │               │               │               │          │
│       └───────────────┼───────────────┼───────────────┘          │
│                       │               │                          │
│              ┌────────▼───────────────▼────────┐                 │
│              │         main.cpp 主调度          │                 │
│              │  · 模式判断 (空地)               │                 │
│              │  · 坡度自适应 (dynMaxV, dynKpV) │                 │
│              │  · 位置目标更新 + 限幅           │                 │
│              │  · 前馈计算 (ff_fade 淡入)      │                 │
│              └────────┬────────────────────────┘                 │
│                       │                                          │
│              ┌────────▼────────────────────────┐                 │
│              │      CascadePID::compute()       │                 │
│              │  · 位置环 P+I (条件积分抗饱和)   │                 │
│              │  · 速度环 P+I+D (微分先行)       │                 │
│              │  · 重力前馈叠加                  │                 │
│              └────────┬────────────────────────┘                 │
│                       │                                          │
│              ┌────────▼────────────────────────┐                 │
│              │    motorSetDifferential(L,R)     │                 │
│              │  · 差分驱动 4 路 TB6612          │                 │
│              └────────┬────────────────────────┘                 │
│                       │                                          │
│              ┌────────▼────────────────────────┐                 │
│              │    Safety::runtimeMonitor()      │                 │
│              │  · 信号/倾角/堵转/IMU 检测      │                 │
│              └─────────────────────────────────┘                 │
└──────────────────────────────────────────────────────────────────┘
```

---

## 7. 关键设计决策

### 7.1 为什么使用串级 PID？

单级位置 PID 在面对坡道时表现不佳——电机需要同时克服位置误差和重力分量，参数难以同时适应平地和坡道。串级结构将速度控制从位置控制中解耦：位置环只关心"要跑多快"，速度环只关心"要出多少力"，内环可在位置环不感知的情况下动态调节力矩。

### 7.2 为什么采用条件积分抗饱和而非反计算法？

反计算法 (back-calculation) 需要额外调一个退饱和增益参数，且对积分限幅值敏感。条件积分法逻辑简单（仅需检查饱和方向与误差方向是否同号），在嵌入式场景下计算开销更低，退饱和速度更快。

### 7.3 为什么使用微分先行？

传统 PID 对误差微分：`d(e)/dt = d(target - actual)/dt`。当位置环输出的 v_target 发生突变（如从静止跳到最大速度），D 项会产生一个巨大的脉冲尖峰，导致电机瞬间剧烈抖动。改为对实际速度微分后，目标突变仅通过 P 和 I 通道平滑响应，消除了微分冲击。

### 7.4 为什么重力前馈需要淡入 (ff_fade)？

空地模式切换瞬间，`angle_pitch` 可能已有较大的非零值。如果直接施加全量前馈，电机接收到一个阶跃式的前馈指令，造成猛冲。淡入机制使前馈从 0 平滑过渡到 1（50 个周期 ≈ 1 秒），避免冲击。

### 7.5 为什么信号丢失检测排除 CH9/CH10 的冻结检测？

CH9 (模式开关) 和 CH10 (清洁开关) 是开关通道，正常使用时就是恒定不变的。如果对它们也做冻结检测，会在正常遥控时误报信号丢失。只对模拟摇杆通道 (CH1/CH2) 做冻结检测。

### 7.6 为什么位置误差超限不直接触发急停？

`FAULT_POS_OVERRUN` 是症状类告警而非根本性故障。它可能由坡道过陡、轮子打滑等暂时性工况触发。直接急停会导致机器人频繁意外停车。当前策略是记录告警但不切断动力，让操作者或更高层逻辑决定如何处理。

---

## 8. 文件索引

| 文件 | 行数 | 职责 |
|------|------|------|
| [platformio.ini](platformio.ini) | 17 | PlatformIO 项目配置 |
| [src/config.h](src/config.h) | 97 | 全局配置：引脚、参数、常量 |
| [src/main.cpp](src/main.cpp) | 224 | 主控入口：setup/loop, 模式调度, PID 编排 |
| [src/pid.h](src/pid.h) | 54 | PID 结构体定义与接口声明 |
| [src/pid.cpp](src/pid.cpp) | 72 | 串级 PID 实现：条件积分抗饱和, 微分先行 |
| [src/motor.h](src/motor.h) | 25 | 电机驱动接口声明 |
| [src/motor.cpp](src/motor.cpp) | 70 | TB6612 四路电机驱动实现 |
| [src/encoder.h](src/encoder.h) | 41 | 编码器接口与原子读取 |
| [src/encoder.cpp](src/encoder.cpp) | 27 | 编码器 ISR 与初始化 |
| [src/receiver.h](src/receiver.h) | 19 | 接收机接口声明 |
| [src/receiver.cpp](src/receiver.cpp) | 43 | PX4 PWM 解码 ISR |
| [src/imu.h](src/imu.h) | 21 | IMU 接口声明 |
| [src/imu.cpp](src/imu.cpp) | 140 | MPU6050 读取 + 互补滤波 + I2C 恢复 |
| [src/autoclean.h](src/autoclean.h) | 39 | 清扫状态机接口声明 |
| [src/autoclean.cpp](src/autoclean.cpp) | 113 | "弓"字形清扫状态机实现 |
| [src/safety.h](src/safety.h) | 78 | 安全模块接口与故障枚举 |
| [src/safety.cpp](src/safety.cpp) | 292 | 安全监控：POST, 急停, 堵转检测, 看门狗 |

---

> **文档版本**: v1.0
> **适用代码版本**: V12 Modular (bf7be83)
> **编写日期**: 2026-07-03
